# Build / artefatos PyInstaller
build/
dist/
__pycache__/
*.pyc

# Estado do usuário (não versionar config da máquina local)
config.json

# PresentMon baixado pelo build (artefato terceiro, MIT)
PresentMon.exe

# Spec gerada automaticamente pelo PyInstaller
monitor-leve.spec